#!/usr/bin/env bash
#
# Print all TODOs in the current branch relative to the specified branch

target=${1-main}

# git diff --color=always "$target" | grep -i todo --context=4
git diff --color=always -G 'todo' -i --exit-code "$target"
